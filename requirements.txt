# UnifyMat — SIH 26099 pipeline + dashboard
# Python 3.13. Versions pinned to the verified build (supply-chain hygiene:
# no surprise upgrades; matches the venv the demo runs on). The app runs
# fully offline if sentence-transformers cannot load (automatic TF-IDF
# fallback), so the heavy deps below the line are optional but recommended
# for embedding-quality matching.

streamlit==1.63.0
plotly==7.0.0
pandas==3.0.5
numpy==2.5.2
scikit-learn==1.9.0
RapidFuzz==3.14.6

# Optional — semantic embedding backend (auto-falls back to TF-IDF if absent)
sentence-transformers==6.0.1
torch==2.14.0
